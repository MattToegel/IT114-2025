package Memory.Common;

public enum Phase {
    READY, // pre-setup phase
    IN_PROGRESS, // example phase that'll be renamed/removed later
    END_TURN_DELAY, // delay between turns (for animation purposes)
}